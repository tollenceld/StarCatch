# 当前任务：锁定卫星摘要面板视觉收口

状态：已完成（2026-08-09）

## 目标

修复锁定卫星底部摘要面板的关闭语义、操作控件层级和过量留白，在不改变锁定、保持、收起与解除锁定逻辑的前提下，使面板更紧凑、精确并符合 StarCatch 的仪器界面语言。

## 已完成

- 将右上角裸露的 `xmark` 改为带弱边界的向下收起控件，明确“隐藏面板但保留锁定”的语义。
- 收起控件改为浮层布局：可见控件为 28pt，点击热区保持 44pt，不再把状态行强制撑高。
- 压缩标题、说明、遥测与操作区之间的纵向间距，并收紧卡片水平内边距和遥测容器高度。
- 保留两行任务摘要和三项关键遥测，没有删除用户判断目标所需的信息。
- 将底部文字工具栏重构为明确的操作组：左侧为带边界的“查看档案”主操作，右侧为解除锁定与官网的图标型辅助入口。
- 解除锁定改用 `lock.open`，官网改用 `safari`，并为两者保留完整无障碍标签与操作提示。
- 继续支持点击面板保持详情、向下滑动收起、重新展开详情以及独立解除锁定；本轮没有增加新的呈现状态。

## 主要涉及文件

- `StarCatch/Archive/ArchiveOverlay.swift`
- `CURRENT_TASK.md`

## 验证

- iPhone 17 Pro / iOS 26.3 模拟器 Debug 构建与启动通过。
- 使用 `--previewLockedTarget` 直接检查锁定态：面板高度、状态行、两行摘要、三项遥测、主操作与两个辅助入口均正常显示，无截断或布局溢出。
- 模拟器确认右上角收起控件不再影响头部行高，底部操作具有稳定边界和一致基线。
- `git diff --check` 通过。

## 下一步

- 真机确认 44pt 收起热区与向下滑动手势不会互相争抢。
- 如果后续继续调整，只修改视觉透明度或文字权重；锁定、收起和解除锁定应继续保持三个独立语义。
